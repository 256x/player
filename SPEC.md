# Literal Player — 仕様メモ

## Literal シリーズについて

個人開発者 256x による Android アプリシリーズ。共通思想は「余計なものを削ぎ落としたテキストベース UI」。

| アプリ | 概要 | パッケージ名 |
|---|---|---|
| **Literal Launcher** | テキストのみのホームランチャー。10スロット固定、アイコンなし | fumi.day.literallauncher |
| **Literal Memo** | ファイルベースの Markdown メモアプリ。Git sync 対応 | fumi.day.literalmemo |
| **Literal Agenda** | テキストベースのスケジュール管理。Git sync・iCal import 対応 | fumi.day.literalagenda |
| **Literal Player** | テキストベースの音声プレイヤー | fumi.day.literalplayer |

## コンセプト

Literal シリーズの音楽・音声プレイヤー。

- アルバムアートなし。テキストのみ。
- ファイル名・メタデータで管理。
- 軽量・シンプル・余計な機能なし。
- 音楽・ラジオ録音・ポッドキャスト・オーディオブック用途を想定。

## 対応フォーマット

mp3 / m4a / aac / opus / flac / ogg / wav

## 画面構成

### 1. 初期起動画面（フォルダ設定）

- 初回起動時のみ表示
- 複数のルートフォルダを追加・削除できる
- 設定後はリスト画面へ遷移

---

### 2. リスト画面（メイン）

UI 参考: LiteralMemo の `MemoListScreen`

```
┌─────────────────────────────────────┐
│ Literal Player          📁  ≡  ⚙   │
├─────────────────────────────────────┤
│  Artist │ Album │ All               │
├─────────────────────────────────────┤
│                                     │
│  （タブ別コンテンツ）               │
│                                     │
├─────────────────────────────────────┤
│  🔍 Search...                       │
└─────────────────────────────────────┘
```

**TopAppBar アイコン（右から）:**
- `⚙` 設定
- `≡` ソート（ファイル名順 / 更新日順 / サイズ順）
- `📁` フォルダ選択（登録済みルートフォルダ一覧 → 選択でそのフォルダ内を表示）

---

### 3. タブ仕様

#### Artist タブ（デフォルト）

アーティスト名のリストのみ表示。タップでアーティスト詳細へ。

```
│  Bruno Mars                    >    │
│  The Beatles                   >    │
│  Unknown Artist                >    │
```

**アーティスト詳細画面:**
- TopAppBar 左: ← / 中央: アーティスト名
- アルバム名（タップ → アルバム再生） + その下に曲リスト（タップ → 曲再生）
- 曲の長押し → Favorites 登録シート

```
│  Doo-Wops & Hooligans               │ ← タップでアルバム再生
│    Just The Way You Are    3:50     │ ← タップで曲再生
│    Grenade                 3:42     │
```

#### Album タブ

アルバム名 ＋ 小さめのアーティスト名。タップでアルバム再生。

```
│  Doo-Wops & Hooligans               │
│  Bruno Mars                         │
```

#### All タブ

曲名のみのフラットリスト。タップで曲再生。長押しで Favorites 登録。

```
│  Come Together                      │
│  Just The Way You Are               │
│  radio_20240315.mp3                 │
```

**メタデータなしの場合:** Unknown Artist / Unknown Album でグループ化

---

### 4. Favorites 登録シート（長押し）

```
┌─────────────────────────────────────┐
│  Just The Way You Are               │
│  ─────────────────────────────────  │
│  + New favorites list               │
│  ─────────────────────────────────  │
│  ♥ Morning playlist                 │
│  ♥ Workout                          │
└─────────────────────────────────────┘
```

---

### 5. プレイヤー画面

```
┌─────────────────────────────────────┐
│ ←  Literal Player                   │
├─────────────────────────────────────┤
│                                     │
│  Just The Way You Are               │
│  Bruno Mars                         │
│  Doo-Wops & Hooligans · 2010        │
│                                     │
│  ▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░  │  ← 大きめシークバー
│  1:18                        3:50   │
│                                     │
│     ⏪    ⏮    ▶/⏸   ⏭    ⏩     │
│   (長押) 前曲  再生  次曲 (長押)   │
│                                     │
│  🔀  🔁                  1.0x ▾   │
└─────────────────────────────────────┘
```

**コントロールボタン:**
| ボタン | 動作 | 長押し |
|---|---|---|
| ⏪ | 秒スキップ後退 | 大スキップ後退 |
| ⏮ | 前曲 | — |
| ▶/⏸ | 再生 / 一時停止 | — |
| ⏭ | 次曲 | — |
| ⏩ | 秒スキップ前進 | 大スキップ前進 |

**再生速度プリセット（固定）:** 0.8 / 1.0 / 1.2 / 1.4 / 1.6 / 1.8 / 2.0

**スキップ秒数（設定で変更可）:**
- 短スキップ: デフォルト 10 秒
- 長スキップ（長押し）: デフォルト 30 秒

---

## 再生機能

| 機能 | 仕様 |
|---|---|
| キュー | 順番 / シャッフル / リピート（1曲・全体） |
| 再生位置保存 | ファイルごとに保存、次回タップで再開 |
| バックグラウンド再生 | 対応。通知コントローラーから操作可能 |

## メタデータ

- ID3タグ等から可能な限り取得・表示
- 取得できない項目は Unknown Artist / Unknown Album で代替

## 設定項目

- ルートフォルダ管理（追加・削除）
- 短スキップ秒数
- 長スキップ秒数（長押し）
- テーマ（LiteralMemo に準拠）
